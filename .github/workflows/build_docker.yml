name: Build sdr-ingestion-rest image

on:
  workflow_dispatch:
  repository_dispatch:

jobs:
  build-docker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: echo
        run: 'echo "field: ${{ github.event.client_payload.service }}"'